# Git Rules

- **Never push to any remote repository (`git push`) without explicit user permission.**
- Always wait for the user to explicitly request or confirm before running `git push`.
- Local commits, builds, tests, and file modifications are permitted, but remote operations must be user-approved.
